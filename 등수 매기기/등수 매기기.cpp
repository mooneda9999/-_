#include <stdio.h>
#include <vector>
using namespace std;

int main(){
	int N, x;
	vector<int> v, n;
	scanf("%d", &N);
	for(int i = 0; i < N;  i++){
		scanf("%d", &x);
		v.push_back(x);
	}
	for(int j = 0; j < N - 1;  j++){
		for(int k = 0; k < N - 1;  k++){
			if()
		}
	}
	
}
